- shell:
    - 
      command: brew bundle --cleanup --no-lock --file ~/.Brewfile
      stdout: true
      stderr: true

- link:
    ~/.vim: vim
    ~/.config/starship.toml: starship.toml
    ~/.gitconfig: gitconfig
    ~/.hushlogin: hushlogin
    ~/.zshrc: zshrc
    /usr/local/bin/lilypond: lilypond
