- shell:
    - brew bundle --cleanup --no-lock --file ~/.Brewfile

- link:
    ~/.vim: vim
    ~/.config/starship.toml: starship.toml
    ~/.gitconfig: gitconfig
    ~/.hushlogin: hushlogin
    ~/.zshrc: zshrc
