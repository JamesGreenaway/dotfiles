- shell:
    - 
      command: brew bundle --cleanup --no-lock --file ~/.Brewfile
      stdout: true
      stderr: true

- link:
    ~/.Brewfile: Brewfile
    ~/.vim: vim
    ~/.config/starship.toml: starship.toml
    ~/.gitconfig: gitconfig
    ~/.hushlogin: hushlogin
    ~/.zshrc: zshrc
